<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Scientific-Calculator</title>
    <style>
      /* Your existing CSS */
      * {
        margin: 0;
        padding: 0;
        box-sizing: border-box;
      }
      :root {
        --calculator-bg-color: #f5f5f5;
        --box-shadow: 0px 8px 16px 0px rgba(0, 0, 0, 0.2);
        --btn-height: 3rem;
        --btn-width: 19%;
        --btn-font-size: 1.1rem;
        --btn-font-weight: bold;
        --hover-bg-color: #e7e7e7;
        --shadow-color: #e4dcdc;
        --btn-background: #f9f9fa;
        --btn-highlight: #ffffff;
        --panel-bg-color: #f1f1f1;
      }
      body {
        height: 100vh;
        width: 100vw;
        display: flex;
        justify-content: center;
        align-items: center;
        font-family: Segoe UI, sans-serif;
      }
      
      /* Layout for calculator and panels */
      .calculator-container {
        display: flex;
        max-width: 1000px;
        width: 90%;
        height: 36.5rem;
        gap: 20px;
      }
      
      .calculator {
        height: 36.5rem;
        width: 25rem;
        background-color: var(--calculator-bg-color);
        flex-shrink: 0;
      }
      
      .panels-container {
        display: flex;
        flex-direction: column;
        gap: 10px;
        flex-grow: 1;
        height: 36.5rem;
      }
      
      .panel {
        background-color: var(--panel-bg-color);
        border-radius: 5px;
        flex: 1;
        padding: 10px;
        overflow-y: auto;
      }
      
      .panel-header {
        border-bottom: 1px solid #ccc;
        padding-bottom: 8px;
        margin-bottom: 8px;
        font-weight: bold;
        display: flex;
        justify-content: space-between;
        align-items: center;
      }
      
      .clear-btn {
        background-color: transparent;
        border: none;
        font-size: 0.9rem;
        color: #666;
        cursor: pointer;
      }
      
      .clear-btn:hover {
        color: #333;
        text-decoration: underline;
      }
      
      .history-item, .memory-item {
        padding: 8px 0;
        border-bottom: 1px solid #eee;
        display: flex;
        justify-content: space-between;
      }
      
      .expression {
        color: #666;
      }
      
      .result-value {
        font-weight: bold;
      }
      
      .memory-value {
        font-weight: bold;
      }
      
      .memory-active {
        color: #0078d7 !important;
      }
      
      /* Existing CSS */
      .header {
        height: 14%;
        width: 100%;
        display: flex;
        align-items: center;
        justify-content: flex-end;
        padding: 0 10px;
      }
      .result {
        max-width: 100%;
        overflow-x: auto;
        white-space: nowrap;
        text-align: right;
        font-size: 40px;
      }
      .btn {
        height: 90%;
        width: 100%;
      }
      .toggle-btn {
        width: 100%;
        height: 10%;
        display: flex;
        align-items: center;
      }
      .unit {
        width: var(--btn-width);
        height: var(--btn-height);
        border: none;
        background-color: var(--calculator-bg-color);
        font-size: var(--btn-font-size);
        font-weight: 600;
      }
      .unit:hover {
        background-color: var(--hover-bg-color);
      }
      .scientific-result {
        width: var(--btn-width);
        height: var(--btn-height);
        border: none;
        background-color: var(--background-calc);
        font-size: var(--btn-font-size);
        font-weight: 700;
      }
      .scientific-result:hover {
        background-color: var(--hover-bg-color);
      }
      .memory-btn {
        width: 100%;
        display: flex;
        align-items: center;
        justify-content: center;
        gap: 2%;
      }
      .btn-shadow {
        color: var(--shadow-color);
      }
      .btn-col1 {
        height: var(--btn-height);
        width: var(--btn-width);
        border: none;
        background-color: var(--background-calc);
        font-size: var(--btn-font-size);
        font-weight: var(--btn-font-weight);
      }
      .btn-col1:hover {
        background-color: var(--hover-bg-color);
      }
      .btn-col:hover {
        background-color: var(--hover-bg-color);
      }
      .function-btn {
        display: flex;
        width: 100%;
        height: 3.2rem;
      }
      .btn-row {
        display: grid;
        grid-template-columns: repeat(5, 1fr);
        width: 100%;
        background-color: var(--background-calc);
      }
      .btn-col {
        height: var(--btn-height);
        border: none;
        background-color: var(--btn-background);
        margin-bottom: 2px;
        margin-right: 2px;
        font-size: 1rem;
      }
      .btn-blue {
        background-color: rgb(125, 170, 207);
      }
      .btn-dark {
        background-color: var(--btn-highlight);
        font-weight: bold;
        font-size: 1.2rem;
      }
      
      /* Responsive design */
      @media (max-width: 768px) {
        .calculator-container {
          flex-direction: column;
          height: auto;
          align-items: center;
        }
        
        .panels-container {
          width: 100%;
          height: auto;
          max-height: 300px;
        }
      }
    </style>
  </head>
  <body>
    <div class="calculator-container">
      <main class="calculator" aria-label="Scientific Calculator">
        <div class="header">
          <output class="result" id="calculator-display" aria-live="polite">0</output>
        </div>
        <div class="btn">
          <div class="toggle-btn">
            <button class="unit" aria-label="Toggle between degrees and radians">DEG</button>
            <button class="scientific-result" aria-label="Toggle scientific notation">F-E</button>
          </div>
          <nav class="memory-btn" aria-label="Memory operations">
            <button class="btn-col1 btn-shadow" aria-label="Memory clear" value="MC">MC</button>
            <button class="btn-col1 btn-shadow" aria-label="Memory recall" value="MR">MR</button>
            <button class="btn-col1" aria-label="Memory add" value="M+">M+</button>
            <button class="btn-col1" aria-label="Memory subtract" value="M-">M-</button>
            <button class="btn-col1" aria-label="Memory store" value="MS">MS</button>
          </nav>
          <!-- Rest of your calculator HTML remains unchanged -->
          <!-- Only showing a sample of the buttons to save space -->
          <section class="keypad" aria-label="Calculator keypad">
            <div class="btn-row">
              <button class="btn-col" aria-label="Second function" value="2nd">2<sup>nd</sup></button>
              <button class="btn-col" aria-label="Pi" value="pi">π</button>
              <button class="btn-col" aria-label="Euler's number" value="e">e</button>
              <button class="btn-col" aria-label="Clear all" value="c">C</button>
              <button class="btn-col" aria-label="Backspace" value="backspace">⌫</button>
            </div>
            <!-- Additional rows would go here -->
          </section>
        </div>
      </main>
      
      <div class="panels-container">
        <div class="panel history-panel" id="history-panel">
          <div class="panel-header">
            <span>History</span>
            <button class="clear-btn" id="clear-history">Clear</button>
          </div>
          <div class="history-content" id="history-content">
            <!-- History items will be added here dynamically -->
          </div>
        </div>
        
        <div class="panel memory-panel" id="memory-panel">
          <div class="panel-header">
            <span>Memory</span>
            <button class="clear-btn" id="clear-all-memory">Clear All</button>
          </div>
          <div class="memory-content" id="memory-content">
            <!-- Memory items will be added here dynamically -->
          </div>
        </div>
      </div>
    </div>

    <script>
      let memoryValues = [];
      let calculationHistory = [];
      let result = '';
      let expression = "";
      let temp = 0;
      let isRadian = false;
      let scientificNotation = false;

      // Initialize memory and history panels
      function initializePanels() {
        // Disable MC and MR buttons initially
        document.querySelectorAll('.memory-btn button').forEach(btn => {
          if (btn.textContent.trim() === 'MC' || btn.textContent.trim() === 'MR') {
            btn.classList.remove('memory-active');
            btn.disabled = true;
          }
        });
        
        // Set up event listeners for clear buttons
        document.getElementById('clear-history').addEventListener('click', clearHistory);
        document.getElementById('clear-all-memory').addEventListener('click', clearAllMemory);
      }

      function display() {
        const resultElement = document.querySelector('.result');
          resultElement.textContent = expression || '0';
      }
      

      // Update history panel
      function updateHistoryPanel(expr, result) {
        if (expr !== "" && expr !== "Error") {
          const historyItem = {
            expression: expr,
            result: result
          };
          
          calculationHistory.push(historyItem);
          
          // Update the history panel UI
          const historyContent = document.getElementById('history-content');
          const itemElement = document.createElement('div');
          itemElement.className = 'history-item';
          itemElement.innerHTML = `
            <div class="expression">${expr} =</div>
            <div class="result-value">${result}</div>
          `;
          
          // Add click event to reuse the expression
          itemElement.addEventListener('click', function() {
            expression = expr;
            display();
          });
          
          historyContent.prepend(itemElement);
        }
      }

      // Update memory panel
      function updateMemoryPanel() {
        const memoryContent = document.getElementById('memory-content');
        memoryContent.innerHTML = '';
        
        memoryValues.forEach((memValue, index) => {
          const itemElement = document.createElement('div');
          itemElement.className = 'memory-item';
          itemElement.innerHTML = `
            <div class="memory-label">M${index + 1}</div>
            <div class="memory-value">${memValue}</div>
          `;
          
          // Add click event to recall this value
          itemElement.addEventListener('click', function() {
            expression = String(memValue);
            display();
          });
          
          memoryContent.appendChild(itemElement);
        });
      }

      // Clear history
      function clearHistory() {
        calculationHistory = [];
        document.getElementById('history-content').innerHTML = '';
      }

      // Clear all memory
      function clearAllMemory() {
        memoryValues = [];
        updateMemoryPanel();
        updateMemoryButtons();
      }

      // Update memory buttons status
      function updateMemoryButtons() {
        const hasMemory = memoryValues.length > 0;
        
        document.querySelectorAll('.memory-btn button').forEach(btn => {
          if (btn.textContent.trim() === 'MC' || btn.textContent.trim() === 'MR') {
            if (hasMemory) {
              btn.classList.add('memory-active');
              btn.disabled = false;
            } else {
              btn.classList.remove('memory-active');
              btn.disabled = true;
            }
          }
        });
      }

      document.querySelectorAll('.btn-col, .btn-col1, .btn-blue, .btn-dark').forEach(btn => {
        btn.addEventListener('click', function() {
          const value = this.value || this.textContent.trim();
          if (value) {
            btnhandler(value);
          }
        });
      });

      function btnhandler(val) {
        let oldExpression = expression;
        
        if (val === 'c') {
          expression = '';
        }
        else if (val === '=') {
          try {
            temp = evaluateExpression(expression);
            result = temp;
            
            // Add to history when equals is pressed
            updateHistoryPanel(expression, result);
            
            expression = String(temp);
          } catch (error) {
            expression = "Error";
          }
        }
        else if (val === 'backspace') {
          if (expression.length === 1 || expression === "Error") {
            expression = '0';
          } else {
            expression = expression.slice(0, -1);
          }
        }
        else if (val === 'pi') {
          insertValue(Math.PI);
        }
        else if (val === 'e') {
          insertValue(Math.E);
        }
        else if (val === 'square') {
          try {
            temp = Math.pow(evaluateExpression(expression), 2);
            expression = String(temp);
          } catch (error) {
            expression = "Error";
          }
        }
        else if (val === 'squareroot') {
          try {
            temp = Math.sqrt(evaluateExpression(expression));
            expression = String(temp);
          } catch (error) {
            expression = "Error";
          }
        }
        else if (val === 'MC') {
          clearAllMemory();
        }
        else if (val === 'MR') {
          if (memoryValues.length > 0) {
            expression = String(memoryValues[memoryValues.length - 1]);
          }
        }
        else if (val === 'M+') {
          try {
            const currentValue = evaluateExpression(expression);
            if (memoryValues.length > 0) {
              memoryValues[memoryValues.length - 1] += currentValue;
            } else {
              memoryValues.push(currentValue);
            }
            updateMemoryPanel();
            updateMemoryButtons();
          } catch (error) {
            expression = "Error";
          }
        }
        else if (val === 'M-') {
          try {
            const currentValue = evaluateExpression(expression);
            if (memoryValues.length > 0) {
              memoryValues[memoryValues.length - 1] -= currentValue;
            } else {
              memoryValues.push(-currentValue);
            }
            updateMemoryPanel();
            updateMemoryButtons();
          } catch (error) {
            expression = "Error";
          }
        }
        else if (val === 'MS') {
          try {
            const currentValue = evaluateExpression(expression);
            memoryValues.push(currentValue);
            updateMemoryPanel();
            updateMemoryButtons();
          } catch (error) {
            expression = "Error";
          }
        }
        else {
          if (expression === '0' || expression === "Error") {
            expression = val;
          } else {
            expression += val;
          }
        }
        
        display();
      }

      function evaluateExpression(expr) {
        expr = expr.replace(/\^/g, '**');
        
        try {
          return eval(expr);
        } catch (error) {
          throw new Error("Invalid expression");
        }
      }

      function insertValue(value) {
        if (expression === '0' || expression === "Error") {
          expression = String(value);
        } else {
          if (/[\d\)]$/.test(expression)) {
            expression += '*' + value;
          } else {
            expression += value;
          }
        }
      }

      document.querySelector('.unit').addEventListener('click', function() {
        isRadian = !isRadian;
        this.textContent = isRadian ? 'RAD' : 'DEG';
      });

      document.querySelector('.scientific-result').addEventListener('click', function() {
        scientificNotation = !scientificNotation;
        display();
      });

      document.addEventListener('keydown', function(event) {
        const key = event.key;
        
        if (/[0-9]/.test(key)) {
          btnhandler(key);
        } else if (key === '+' || key === '-' || key === '.' || key === '(' || key === ')') {
          btnhandler(key);
        } else if (key === '*') {
          btnhandler('*');
        } else if (key === '/') {
          btnhandler('divide');
        } else if (key === '%') {
          btnhandler('mod');
        } else if (key === 'Enter' || key === '=') {
          btnhandler('=');
        } else if (key === 'Backspace') {
          btnhandler('backspace');
        } else if (key === 'Escape') {
          btnhandler('c');
        } else if (key === '^') {
          btnhandler('xy');
        }
      });

      // Initialize on page load
      document.addEventListener('DOMContentLoaded', function() {
        initializePanels();
        display();
      });
    </script>
  </body>
</html>












* {
  margin: 0;
  padding: 0;
  box-sizing: border-box;
}
:root {
  --calculator-bg-color: #f5f5f5;
  --box-shadow: 0px 8px 16px 0px rgba(0, 0, 0, 0.2);
  --btn-height: 3rem;
  --btn-width: 19%;
  --btn-font-size: 1.1rem;
  --btn-font-weight: bold;
  --hover-bg-color: #e7e7e7;
  --shadow-color: #e4dcdc;
  --btn-background: #f9f9fa;
  --btn-highlight: #ffffff;
}
body {
  height: 100vh;
  width: 100vw;
  display: flex;
  justify-content: center;
  align-items: center;
  font-family: Segoe UI, sans-serif;
}
.calculator {
  height: 36.5rem;
  width: 25rem;
  background-color: var(--calculator-bg-color);
}
.header {
  height: 14%;
  width: 100%;
  display: flex;
  align-items: center;
  justify-content: flex-end;
  padding: 0 10px;
}
.result {
  max-width: 100%;
  overflow-x: auto;
  white-space: nowrap;
  text-align: right;
  font-size: 40px;
}
.btn {
  height: 90%;
  width: 100%;
}
.toggle-btn {
  width: 100%;
  height: 10%;
  display: flex;
  align-items: center;
}
.unit {
  width: var(--btn-width);
  height: var(--btn-height);
  border: none;
  background-color: var(--calculator-bg-color);
  font-size: var(--btn-font-size);
  font-weight: 600;
}
.unit:hover {
  background-color: var(--hover-bg-color);
}
.scientific-result {
  width: var(--btn-width);
  height: var(--btn-height);
  border: none;
  background-color: var(--background-calc);
  font-size: var(--btn-font-size);
  font-weight: 700;
}
.scientific-result:hover {
  background-color: var(--hover-bg-color);
}
.memory-btn {
  width: 100%;
  display: flex;
  align-items: center;
  justify-content: center;
  gap: 2%;
}
.btn-shadow {
  color: var(--shadow-color);
}
.btn-col1 {
  height: var(--btn-height);
  width: var(--btn-width);
  border: none;
  background-color: var(--background-calc);
  font-size: var(--btn-font-size);
  font-weight: var(--btn-font-weight);
}
.btn-col1:hover {
  background-color: var(--hover-bg-color);
}
.btn-col:hover {
  background-color: var(--hover-bg-color);
}
.function-btn {
  display: flex;
  width: 100%;
  height: 3.2rem;
}
.btn-row {
  display: grid;
  grid-template-columns: repeat(5, 1fr);
  width: 100%;
  background-color: var(--background-calc);
}
.btn-col {
  height: var(--btn-height);
  border: none;
  background-color: var(--btn-background);
  margin-bottom: 2px;
  margin-right: 2px;
  font-size: 1rem;
}
.btn-blue {
  background-color: rgb(125, 170, 207);
}
.btn-dark {
  background-color: var(--btn-highlight);
  font-weight: bold;
  font-size: 1.2rem;
}
#arrow-down {
  height: 1rem;
}
span:hover {
  cursor: default;
}
.func-dropdown {
  position: relative;
  height: var(--btn-height);
  width: 36%;
}
.trigno-dropdown {
  position: relative;
  height: var(--btn-height);
  width: 36%;
}
.trigno-dropbtn {
  border: none;
  width: 100%;
  height: 100%;
  background-color: var(--background-calc);
  display: flex;
  align-items: center;
  justify-content: center;
  gap: 3px;
  font-size: var(--btn-font-size);
}
.trigno-dropbtn > img {
  height: 1rem;
  width: 1rem;
}
.func-dropbtn {
  border: none;
  width: 100%;
  height: 100%;
  background-color: var(--background-calc);
  display: flex;
  align-items: center;
  justify-content: center;
  gap: 5px;
  font-size: var(--btn-font-size);
}
.func-dropbtn > img {
  height: 1rem;
  width: 1rem;
}
.trigno-dropbtn:hover,
.func-dropbtn:hover {
  background-color: var(--hover-bg-color);
}
.trigno-dropdown-content,
.func-dropdown-content {
  display: none;
  position: absolute;
  background-color: #f1f1f1;
  width: 100%;
  box-shadow: 0px 8px 16px 0px rgba(0, 0, 0, 0.2);
  z-index: 1;
}
.trigno-dropdown-content a,
.func-dropdown-content a {
  color: black;
  padding: 12px 16px;
  text-decoration: none;
  display: block;
}
.trigno-dropdown-content a:hover,
.func-dropdown-content a:hover {
  background-color: #ddd;
}
.show-trigno,
.show-func {
  display: block;
}


.calculator-container {
  display: flex;
  max-width: 1000px;
  width: 90%;
  height: 36.5rem;
  gap: 20px;
}

.calculator {
  height: 36.5rem;
  width: 25rem;
  background-color: var(--calculator-bg-color);
  flex-shrink: 0;
}

.panels-container {
  display: flex;
  flex-direction: column;
  gap: 10px;
  flex-grow: 1;
  height: 36.5rem;
}

.panel {
  background-color: var(--panel-bg-color);
  border-radius: 5px;
  flex: 1;
  padding: 10px;
  overflow-y: auto;
}

.panel-header {
  border-bottom: 1px solid #ccc;
  padding-bottom: 8px;
  margin-bottom: 8px;
  font-weight: bold;
  display: flex;
  justify-content: space-between;
  align-items: center;
}

.clear-btn {
  background-color: transparent;
  border: none;
  font-size: 0.9rem;
  color: #666;
  cursor: pointer;
}

.clear-btn:hover {
  color: #333;
  text-decoration: underline;
}

.history-item, .memory-item {
  padding: 8px 0;
  border-bottom: 1px solid #eee;
  display: flex;
  justify-content: space-between;
}

.expression {
  color: #666;
}

.result-value {
  font-weight: bold;
}

.memory-value {
  font-weight: bold;
}

.memory-active {
  color: #0078d7 !important;
}

@media (max-width: 768px) {
  .calculator-container {
    flex-direction: column;
    height: auto;
    align-items: center;
  }
  
  .panels-container {
    width: 100%;
    height: auto;
    max-height: 300px;
  }
}
@media (max-width: 500px) {
  .calculator {
    width: 90%;
  }
}
@media (max-height: 650px) {
  .calculator {
    height: 100%;
  }
}
@media (max-width: 425px) {
  .trigno-dropdown {
    width: 50%;
  }
  .func-dropdown {
    width: 50%;
  }
}





<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Scientific-Calculator</title>
    <link rel="stylesheet" href="./style/style.css" />
    <link rel="shortcut icon" href="./assets/Media.jpeg" type="image/x-icon" />
    <meta
      name="description"
      content="This is the Scientific Calculator site, in which you can perform various Scientific Calculations."
    />
    <!-- Open Graph meta tags -->
    <meta property="og:title" content="Scientific Calculator" />
    <meta
      property="og:description"
      content="This is the Scientific Calculator site, in which you can perform various Scientific Calculations."
    />
    <meta property="og:image" content="./assets/Media.jpeg" />
    <meta property="og:image:alt" content="Scientific Calculator Cover Image" />
    <!-- Twitter Card meta tags -->
    <meta name="twitter:title" content="Scientific Calculator" />
    <meta
      name="twitter:description"
      content="This is the Scientific Calculator site, in which you can perform various Scientific Calculations."
    />
    <meta name="twitter:url" content="/" />
    <meta name="twitter:image:src" content="./assets/Media.jpeg" />
    <meta
      name="twitter:image:alt"
      content="Scientific Calculator Cover Image"
    />
  </head>
  <body>
  <div class="calculator-container">  
    <main class="calculator" aria-label="Scientific Calculator">
      <div class="header">
        <output class="result" id="calculator-display" aria-live="polite"
          >0</output
        >
      </div>
      <div class="btn">
        <div class="toggle-btn">
          <button class="unit" aria-label="Toggle between degrees and radians">
            DEG
          </button>
          <button
            class="scientific-result"
            aria-label="Toggle scientific notation"
          >
            F-E
          </button>
        </div>
        <nav class="memory-btn" aria-label="Memory operations">
          <button class="btn-col1 btn-shadow" aria-label="Memory clear">
            MC
          </button>
          <button class="btn-col1 btn-shadow" aria-label="Memory recall">
            MR
          </button>
          <button class="btn-col1" aria-label="Memory add">M+</button>
          <button class="btn-col1" aria-label="Memory subtract">M-</button>
          <button class="btn-col1" aria-label="Memory store">MS</button>
        </nav>
        <nav class="function-btn" aria-label="Function menus">
          <div class="trigno-dropdown" aria-label="Trigonometry functions menu">
            <button class="trigno-dropbtn">
              <img
                src="./assets/right-angle-svgrepo-com.svg"
                alt=""
                aria-hidden="true"
              />
              <span>Trignometry</span>
              <img
                src="./assets/arrow-down.svg"
                alt=""
                aria-hidden="true"
                id="arrow-down"
              />
            </button>
            <div id="trigno-myDropdown" class="trigno-dropdown-content">
              <a href="#" value="sin">Sin</a>
              <a href="#" value="cos">Cos</a>
              <a href="#" value="tan">tan</a>
            </div>
          </div>
          <div class="func-dropdown" aria-label="Functions menu">
            <button class="func-dropbtn">
              <img
                src="./assets/function.svg"
                alt=""
                aria-hidden="true"
                id="arrow-down"
              />
              <span>Functions</span>
              <img
                src="./assets/arrow-down.svg"
                alt=""
                aria-hidden="true"
                id="arrow-down"
              />
            </button>
            <div id="func-myDropdown" class="func-dropdown-content">
              <a href="#" value="abs">abs</a>
              <a href="#" value="ceil">Ceil</a>
              <a href="#" value="floor">floor</a>
            </div>
          </div>
        </nav>
        <section class="keypad" aria-label="Calculator keypad">
          <div class="btn-row">
            <button class="btn-col" aria-label="Second function" value="2nd">
              2<sup>nd</sup>
            </button>
            <button class="btn-col" aria-label="Pi" value="pi">π</button>
            <button class="btn-col" aria-label="Euler's number" value="e">e</button>
            <button class="btn-col" aria-label="Clear all" value="c">C</button>
            <button class="btn-col" aria-label="Backspace" value="backspace">⌫</button>
          </div>
          <div class="btn-row">
            <button class="btn-col" aria-label="x squared" value="square">
              x<sup>2</sup>
            </button>
            <button class="btn-col" aria-label="One divided by x" value="inverse">1/x</button>
            <button class="btn-col" aria-label="Absolute value" value="abs">|x|</button>
            <button class="btn-col" aria-label="Exponential" value="exponential">exp</button>
            <button class="btn-col" aria-label="Modulo" value="mod">mod</button>
          </div>
          <div class="btn-row">
            <button class="btn-col" aria-label="Square root" value="squareroot">√</button>
            <button class="btn-col" aria-label="Open parenthesis" value="(">(</button>
            <button class="btn-col" aria-label="Close parenthesis" value=")">)</button>
            <button class="btn-col" aria-label="Factorial" value="factorial">n!</button>
            <button class="btn-col" aria-label="Divide" value="divide">÷</button>
          </div>
          <div class="btn-row">
            <button class="btn-col" aria-label="x raised to power y" value="xy">
              x<sup>y</sup>
            </button>
            <button class="btn-col btn-dark" aria-label="Seven" value="7">7</button>
            <button class="btn-col btn-dark" aria-label="Eight" value="8">8</button>
            <button class="btn-col btn-dark" aria-label="Nine" value="9">9</button>
            <button class="btn-col" aria-label="Multiply" value="*">×</button>
          </div>
          <div class="btn-row">
            <button class="btn-col" aria-label="Ten to the power of x" value="10x">
              10<sup>x</sup>
            </button>
            <button class="btn-col btn-dark" aria-label="Four" value="4">4</button>
            <button class="btn-col btn-dark" aria-label="Five" value="5">5</button>
            <button class="btn-col btn-dark" aria-label="Six" value="6">6</button>
            <button class="btn-col" aria-label="Subtract" value="-">-</button>
          </div>
          <div class="btn-row">
            <button class="btn-col" aria-label="Logarithm base 10" value="log">log</button>
            <button class="btn-col btn-dark" aria-label="One" value="1">1</button>
            <button class="btn-col btn-dark" aria-label="Two" value="2">2</button>
            <button class="btn-col btn-dark" aria-label="Three" value="3">3</button>
            <button class="btn-col" aria-label="Add" value="+">+</button>
          </div>
          <div class="btn-row">
            <button class="btn-col" aria-label="Natural logarithm" value="ln">ln</button>
            <button class="btn-col btn-dark" aria-label="Change sign" value="+/-">+/-</button>
            <button class="btn-col btn-dark" aria-label="Zero" value="0">0</button>
            <button class="btn-col btn-dark" aria-label="Decimal point" value=".">.</button>
            <button class="btn-col btn-blue" aria-label="Equals" value="=">=</button>
          </div>
        </section>
      </div>
    </main>
    <div class="panels-container">
      <div class="panel history-panel" id="history-panel">
        <div class="panel-header">
          <span>History</span>
          <button class="clear-btn" id="clear-history">Clear</button>
        </div>
        <div class="history-content" id="history-content">
          <!-- History items will be added here dynamically -->
        </div>
      </div>
      
      <div class="panel memory-panel" id="memory-panel">
        <div class="panel-header">
          <span>Memory</span>
          <button class="clear-btn" id="clear-all-memory">Clear All</button>
        </div>
        <div class="memory-content" id="memory-content">
          <!-- Memory items will be added here dynamically -->
        </div>
      </div>
    </div>
  </div>

    <script src="script.js"></script>
    <script src="utility.js"></script>
  </body>
</html>

* {
  margin: 0;
  padding: 0;
  box-sizing: border-box;
}
:root {
  --calculator-bg-color: #f5f5f5;
  --box-shadow: 0px 8px 16px 0px rgba(0, 0, 0, 0.2);
  --btn-height: 3rem;
  --btn-width: 19%;
  --btn-font-size: 1.1rem;
  --btn-font-weight: bold;
  --hover-bg-color: #e7e7e7;
  --shadow-color: #e4dcdc;
  --btn-background: #f9f9fa;
  --btn-highlight: #ffffff;
}
body {
  height: 100vh;
  width: 100vw;
  display: flex;
  justify-content: center;
  align-items: center;
  font-family: Segoe UI, sans-serif;
}
.calculator {
  height: 36.5rem;
  width: 25rem;
  background-color: var(--calculator-bg-color);
}
.header {
  height: 14%;
  width: 100%;
  display: flex;
  align-items: center;
  justify-content: flex-end;
  padding: 0 10px;
}
.result {
  max-width: 100%;
  overflow-x: auto;
  white-space: nowrap;
  text-align: right;
  font-size: 40px;
}
.btn {
  height: 90%;
  width: 100%;
}
.toggle-btn {
  width: 100%;
  height: 10%;
  display: flex;
  align-items: center;
}
.unit {
  width: var(--btn-width);
  height: var(--btn-height);
  border: none;
  background-color: var(--calculator-bg-color);
  font-size: var(--btn-font-size);
  font-weight: 600;
}
.unit:hover {
  background-color: var(--hover-bg-color);
}
.scientific-result {
  width: var(--btn-width);
  height: var(--btn-height);
  border: none;
  background-color: var(--background-calc);
  font-size: var(--btn-font-size);
  font-weight: 700;
}
.scientific-result:hover {
  background-color: var(--hover-bg-color);
}
.memory-btn {
  width: 100%;
  display: flex;
  align-items: center;
  justify-content: center;
  gap: 2%;
}
.btn-shadow {
  color: var(--shadow-color);
}
.btn-col1 {
  height: var(--btn-height);
  width: var(--btn-width);
  border: none;
  background-color: var(--background-calc);
  font-size: var(--btn-font-size);
  font-weight: var(--btn-font-weight);
}
.btn-col1:hover {
  background-color: var(--hover-bg-color);
}
.btn-col:hover {
  background-color: var(--hover-bg-color);
}
.function-btn {
  display: flex;
  width: 100%;
  height: 3.2rem;
}
.btn-row {
  display: grid;
  grid-template-columns: repeat(5, 1fr);
  width: 100%;
  background-color: var(--background-calc);
}
.btn-col {
  height: var(--btn-height);
  border: none;
  background-color: var(--btn-background);
  margin-bottom: 2px;
  margin-right: 2px;
  font-size: 1rem;
}
.btn-blue {
  background-color: rgb(125, 170, 207);
}
.btn-dark {
  background-color: var(--btn-highlight);
  font-weight: bold;
  font-size: 1.2rem;
}
#arrow-down {
  height: 1rem;
}
span:hover {
  cursor: default;
}
.func-dropdown {
  position: relative;
  height: var(--btn-height);
  width: 36%;
}
.trigno-dropdown {
  position: relative;
  height: var(--btn-height);
  width: 36%;
}
.trigno-dropbtn {
  border: none;
  width: 100%;
  height: 100%;
  background-color: var(--background-calc);
  display: flex;
  align-items: center;
  justify-content: center;
  gap: 3px;
  font-size: var(--btn-font-size);
}
.trigno-dropbtn > img {
  height: 1rem;
  width: 1rem;
}
.func-dropbtn {
  border: none;
  width: 100%;
  height: 100%;
  background-color: var(--background-calc);
  display: flex;
  align-items: center;
  justify-content: center;
  gap: 5px;
  font-size: var(--btn-font-size);
}
.func-dropbtn > img {
  height: 1rem;
  width: 1rem;
}
.trigno-dropbtn:hover,
.func-dropbtn:hover {
  background-color: var(--hover-bg-color);
}
.trigno-dropdown-content,
.func-dropdown-content {
  display: none;
  position: absolute;
  background-color: #f1f1f1;
  width: 100%;
  box-shadow: 0px 8px 16px 0px rgba(0, 0, 0, 0.2);
  z-index: 1;
}
.trigno-dropdown-content a,
.func-dropdown-content a {
  color: black;
  padding: 12px 16px;
  text-decoration: none;
  display: block;
}
.trigno-dropdown-content a:hover,
.func-dropdown-content a:hover {
  background-color: #ddd;
}
.show-trigno,
.show-func {
  display: block;
}


.calculator-container {
  display: flex;
  max-width: 1000px;
  width: 90%;
  height: 36.5rem;
  gap: 20px;
}

.calculator {
  height: 36.5rem;
  width: 25rem;
  background-color: var(--calculator-bg-color);
  flex-shrink: 0;
}

.panels-container {
  display: flex;
  flex-direction: column;
  gap: 10px;
  flex-grow: 1;
  height: 36.5rem;
}

.panel {
  background-color: var(--panel-bg-color);
  border-radius: 5px;
  flex: 1;
  padding: 10px;
  overflow-y: auto;
}

.panel-header {
  border-bottom: 1px solid #ccc;
  padding-bottom: 8px;
  margin-bottom: 8px;
  font-weight: bold;
  display: flex;
  justify-content: space-between;
  align-items: center;
}

.clear-btn {
  background-color: transparent;
  border: none;
  font-size: 0.9rem;
  color: #666;
  cursor: pointer;
}

.clear-btn:hover {
  color: #333;
  text-decoration: underline;
}

.history-item, .memory-item {
  padding: 8px 0;
  border-bottom: 1px solid #eee;
  display: flex;
  justify-content: space-between;
}

.expression {
  color: #666;
}

.result-value {
  font-weight: bold;
}

.memory-value {
  font-weight: bold;
}

.memory-active {
  color: #0078d7 !important;
}

@media (max-width: 768px) {
  .calculator-container {
    flex-direction: column;
    height: auto;
    align-items: center;
  }
  
  .panels-container {
    width: 100%;
    height: auto;
    max-height: 300px;
  }
}
@media (max-width: 500px) {
  .calculator {
    width: 90%;
  }
}
@media (max-height: 650px) {
  .calculator {
    height: 100%;
  }
}
@media (max-width: 425px) {
  .trigno-dropdown {
    width: 50%;
  }
  .func-dropdown {
    width: 50%;
  }
}



document.querySelector(".trigno-dropdown").addEventListener("click", () => {
  document.getElementById("trigno-myDropdown").classList.toggle("show-trigno");
});
document.querySelector(".func-dropdown").addEventListener("click", () => {
  document.getElementById("func-myDropdown").classList.toggle("show-func");
});

window.onclick = function (event) {
  if (!event.target.closest(".trigno-dropbtn")) {
    if (
      document
        .getElementById("trigno-myDropdown")
        .classList.contains("show-trigno")
    ) {
      document
        .getElementById("trigno-myDropdown")
        .classList.remove("show-trigno");
    }
  }
  if (!event.target.closest(".func-dropbtn")) {
    if (
      document.getElementById("func-myDropdown").classList.contains("show-func")
    ) {
      document.getElementById("func-myDropdown").classList.remove("show-func");
    }
  }
};


let memoryValues = [];
let calculationHistory = [];
let result = '';
let expression = "";
let temp = 0;
let isRadian = false;
let scientificNotation = false;

function display() {
  const resultElement = document.querySelector('.result');
  
  if (scientificNotation && !isNaN(parseFloat(expression))) {
    resultElement.textContent = parseFloat(expression).toExponential();
  } else {
    resultElement.textContent = expression || '0';
  }
}

function clearAllMemory() {
  memoryValues = [];
  updateMemoryPanel();
  updateMemoryButtons();
}

function initializePanels() {
  // Disable MC and MR buttons initially
  document.querySelectorAll('.memory-btn button').forEach(btn => {
    if (btn.textContent.trim() === 'MC' || btn.textContent.trim() === 'MR') {
      btn.classList.remove('memory-active');
      btn.disabled = true;
    }
  });
  
  // Set up event listeners for clear buttons
  document.getElementById('clear-history').addEventListener('click', clearHistory);
  document.getElementById('clear-all-memory').addEventListener('click', clearAllMemory);
}
function updateHistoryPanel(expr, result) {
  if (expr !== "" && expr !== "Error") {
    const historyItem = {
      expression: expr,
      result: result
    };
    
    calculationHistory.push(historyItem);
    
    // Update the history panel UI
    const historyContent = document.getElementById('history-content');
    const itemElement = document.createElement('div');
    itemElement.className = 'history-item';
    itemElement.innerHTML = `
      <div class="expression">${expr} =</div>
      <div class="result-value">${result}</div>
    `;
    
    // Add click event to reuse the expression
    itemElement.addEventListener('click', function() {
      expression = expr;
      display();
    });
    
    historyContent.prepend(itemElement);
  }
}


function updateMemoryPanel() {
  const memoryContent = document.getElementById('memory-content');
  memoryContent.innerHTML = '';
  
  memoryValues.forEach((memValue, index) => {
    const itemElement = document.createElement('div');
    itemElement.className = 'memory-item';
    itemElement.innerHTML = `
      <div class="memory-label">M${index + 1}</div>
      <div class="memory-value">${memValue}</div>
    `;
    
    // Add click event to recall this value
    itemElement.addEventListener('click', function() {
      expression = String(memValue);
      display();
    });
    
    memoryContent.appendChild(itemElement);
  });
}


function clearHistory() {
  calculationHistory = [];
  document.getElementById('history-content').innerHTML = '';
}

function updateMemoryButtons() {
  const hasMemory = memoryValues.length > 0;
  
  document.querySelectorAll('.memory-btn button').forEach(btn => {
    if (btn.textContent.trim() === 'MC' || btn.textContent.trim() === 'MR') {
      if (hasMemory) {
        btn.classList.add('memory-active');
        btn.disabled = false;
      } else {
        btn.classList.remove('memory-active');
        btn.disabled = true;
      }
    }
  });
}

document.querySelectorAll('.btn-col, .btn-col1').forEach(btn => {
  btn.addEventListener('click', function() {
    const value = this.value;
    if (value) {
      btnhandler(value);
    }
  });
});

function btnhandler(val) {
  if (val === 'c') {
    expression = '';
  }
  else if (val === '=') {
    try {
      temp = evaluateExpression(expression);
      result = temp;
      expression = String(temp);
      updateHistoryPanel(expression, result);
    } catch (error) {
      expression = "Error";
    }
  }
  else if (val === 'backspace') {
    if (expression.length === 1 || expression === "Error") {
      expression = '0';
    } else {
      expression = expression.slice(0, -1);
    }
  }
  else if (val === 'pi') {
    insertValue(Math.PI);
  }
  else if (val === 'e') {
    insertValue(Math.E);
  }
  else if (val === 'square') {
    try {
      temp = Math.pow(evaluateExpression(expression), 2);
      expression = String(temp);
    } catch (error) {
      expression = "Error";
    }
  }
  else if (val === 'squareroot') {
    try {
      temp = Math.sqrt(evaluateExpression(expression));
      expression = String(temp);
    } catch (error) {
      expression = "Error";
    }
  }
  else if (val === 'inverse') {
    try {
      temp = 1 / evaluateExpression(expression);
      expression = String(temp);
    } catch (error) {
      expression = "Error";
    }
  }
  else if (val === 'abs') {
    try {
      temp = Math.abs(evaluateExpression(expression));
      expression = String(temp);
    } catch (error) {
      expression = "Error";
    }
  }
  else if (val === 'factorial') {
    try {
      temp = evaluateExpression(expression);
      if (temp < 0 || !Number.isInteger(temp)) {
        expression = "Error";
      } else {
        let fac = 1;
        for (let i = 2; i <= temp; i++) {
          fac = fac * i;
        }
        expression = String(fac);
      }
    } catch (error) {
      expression = "Error";
    }
  }
  else if (val === 'log') {
    try {
      temp = Math.log10(evaluateExpression(expression));
      expression = String(temp);
    } catch (error) {
      expression = "Error";
    }
  }
  else if (val === 'ln') {
    try {
      temp = Math.log(evaluateExpression(expression));
      expression = String(temp);
    } catch (error) {
      expression = "Error";
    }
  }
  else if (val === '10x') {
    try {
      temp = Math.pow(10, evaluateExpression(expression));
      expression = String(temp);
    } catch (error) {
      expression = "Error";
    }
  }
  else if (val === 'xy') {
    expression += '^';
  }
  else if (val === '+' || val === '-' || val === '*' || val === '(' || val === ')' || val === '.') {
    expression += val;
  }
  else if (val === 'divide') {
    expression += '/';
  }
  else if (val === 'mod') {
    expression += '%';
  }
  else if (val === 'sin') {
    try {
      let angle = evaluateExpression(expression);
      if (!isRadian) {
        angle = angle * Math.PI / 180;
      }
      temp = Math.sin(angle);
      expression = String(temp);
    } catch (error) {
      expression = "Error";
    }
  }
  else if (val === 'cos') {
    try {
      let angle = evaluateExpression(expression);
      if (!isRadian) {
        angle = angle * Math.PI / 180;
      }
      temp = Math.cos(angle);
      expression = String(temp);
    } catch (error) {
      expression = "Error";
    }
  }
  else if (val === 'tan') {
    try {
      let angle = evaluateExpression(expression);
      if (!isRadian) {
        angle = angle * Math.PI / 180;
      }
      temp = Math.tan(angle);
      expression = String(temp);
    } catch (error) {
      expression = "Error";
    }
  }
  else if (val === 'ceil') {
    try {
      temp = Math.ceil(evaluateExpression(expression));
      expression = String(temp);
    } catch (error) {
      expression = "Error";
    }
  }
  else if (val === 'floor') {
    try {
      temp = Math.floor(evaluateExpression(expression));
      expression = String(temp);
    } catch (error) {
      expression = "Error";
    }
  }
  else if (val === 'exponential') {
    try {
      temp = Math.exp(evaluateExpression(expression));
      expression = String(temp);
    } catch (error) {
      expression = "Error";
    }
  }
  else if (val === 'MC') {
    clearAllMemory();
  }
  else if (val === 'MR') {
    if (memoryValues.length > 0) {
      expression = String(memoryValues[memoryValues.length - 1]);
    }
  }
  else if (val === 'M+') {
    try {
      const currentValue = evaluateExpression(expression);
      if (memoryValues.length > 0) {
        memoryValues[memoryValues.length - 1] += currentValue;
      } else {
        memoryValues.push(currentValue);
      }
      updateMemoryPanel();
      updateMemoryButtons();
    } catch (error) {
      expression = "Error";
    }
  }
  else if (val === 'M-') {
    try {
      const currentValue = evaluateExpression(expression);
      if (memoryValues.length > 0) {
        memoryValues[memoryValues.length - 1] -= currentValue;
      } else {
        memoryValues.push(-currentValue);
      }
      updateMemoryPanel();
      updateMemoryButtons();
    } catch (error) {
      expression = "Error";
    }
  }
  else if (val === 'MS') {
    try {
      const currentValue = evaluateExpression(expression);
      memoryValues.push(currentValue);
      updateMemoryPanel();
      updateMemoryButtons();
    } catch (error) {
      expression = "Error";
    }
  }
  else if (val === '+/-') {
    try {
      temp = -1 * evaluateExpression(expression);
      expression = String(temp);
    } catch (error) {
      expression = "Error";
    }
  }
  else {
    if (expression === '0' || expression === "Error") {
      expression = val;
    } else {
      expression += val;
    }
  }
  
  display();
}

function evaluateExpression(expr) {
  expr = expr.replace(/\^/g, '**');
  
  try {
    return eval(expr);
  } catch (error) {
    throw new Error("Invalid expression");
  }
}

function insertValue(value) {
  if (expression === '0' || expression === "Error") {
    expression = String(value);
  } else {
    if (/[\d\)]$/.test(expression)) {
      expression += '*' + value;
    } else {
      expression += value;
    }
  }
}


document.querySelector('.unit').addEventListener('click', function() {
  isRadian = !isRadian;
  this.textContent = isRadian ? 'RAD' : 'DEG';
});

document.querySelectorAll('.memory-btn button').forEach(btn => {
  btn.addEventListener('click', function() {
    btnhandler(this.value);
  });
});

document.addEventListener('keydown', function(event) {
  const key = event.key;
  
  if (/[0-9]/.test(key)) {
    btnhandler(key);
  } else if (key === '+' || key === '-' || key === '.' || key === '(' || key === ')') {
    btnhandler(key);
  } else if (key === '*') {
    btnhandler('*');
  } else if (key === '/') {
    btnhandler('divide');
  } else if (key === '%') {
    btnhandler('mod');
  } else if (key === 'Enter' || key === '=') {
    btnhandler('=');
  } else if (key === 'Backspace') {
    btnhandler('backspace');
  } else if (key === 'Escape') {
    btnhandler('c');
  } else if (key === '^') {
    btnhandler('xy');
  }
});

document.addEventListener('DOMContentLoaded', function() {
  initializePanels();
  display();
});

document.querySelectorAll('#trigno-myDropdown a').forEach(item => {
  item.addEventListener('click', function(e) {
    e.preventDefault();
    btnhandler(this.getAttribute('value'));
  });
});

document.querySelectorAll('#func-myDropdown a').forEach(item => {
  item.addEventListener('click', function(e) {
    e.preventDefault();
    btnhandler(this.getAttribute('value'));
  });
});

display();